# Documentation
mkdocs-material

# Install source dependencies (only after .infra-setup exists)
# -r .infra-setup/requirements-dev.txt

# Install local package in editable mode (only after .infra-setup exists)
# -e .infra-setup/src
